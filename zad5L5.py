#!/usr/bin/env python3
'''
def zmien(s, t):
	with open("tekst.txt", "r+") as txt:
		tekst = txt.read()
		zmienione = tekst.replace(s, t)
		txt.truncate(0)
		txt.write(zmienione)
zmien("gitara", "bas")
'''

with 
